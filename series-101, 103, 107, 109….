#include<iostream>
using namespace std;
int main()
{
    int c=0,i,j,n,b=0;
    cin>>n;
    for(i=100;i<=200;i++)
    {
       for(j=1;j<=i;j++)
       {
           if(i%j==0)
           {
               c++;
           }
       }
       if(c<=2)
       {
           cout<<i<<" ";
           b++;
       }
       c=0;
       if(b==n)
       {
           break;
       }
    }
}
