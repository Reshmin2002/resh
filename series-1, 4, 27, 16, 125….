#include<iostream>
using namespace std;
int main()
{
    int a,b,c,i;
    cin>>a;
    for(i=1;i<=a;i++)
    {
        if(i%2==0)
        {
            cout<<i*i<<" ";
        }
        else
        {
            cout<<i*i*i<<" ";
        }
    }
}
